# Annotation Component

## Overview
Processes ingested Nextcloud files, enriches them with metadata, applies classification/tagging, and exposes annotated artifacts to downstream consumers.

## Setup
1. Configure environment variables
2. Install dependencies: `pip install -r requirements.txt`
3. Build and run service: `make build` and `make run`.
